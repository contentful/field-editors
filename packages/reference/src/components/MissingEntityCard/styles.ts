import { css } from 'emotion';

export const card = css({
  position: 'relative',
});

export const squareCard = css({
  display: 'flex',
  alignItems: 'center',
  width: '135px',
  height: '160px',
  textAlign: 'center',
});
