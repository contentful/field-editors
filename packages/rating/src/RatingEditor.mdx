---
name: Rating
route: /rating
menu: Editors
---

# Rating

import { Playground } from 'docz';
import { RatingEditor } from './RatingEditor.tsx';
import { createFakeFieldAPI } from '@contentful/field-editor-test-utils';

## In Action

<Playground>
  {() => {
    const [field] = createFakeFieldAPI();
    return <RatingEditor field={field} isInitiallyDisabled={false} />;
  }}
</Playground>

## Disabled

<Playground>
  {() => {
    const [field] = createFakeFieldAPI();
    return <RatingEditor field={field} isInitiallyDisabled />;
  }}
</Playground>

## More stars

<Playground>
  {() => {
    const [field] = createFakeFieldAPI();
    return (
      <RatingEditor
        field={field}
        isInitiallyDisabled={false}
        parameters={{ instance: { stars: 20 } }}
      />
    );
  }}
</Playground>
