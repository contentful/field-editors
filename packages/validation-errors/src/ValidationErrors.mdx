---
name: Validation Errors
route: /validation-errors
menu: Editors
order: 1
---

import Readme from '../README.md';

<Readme />

import { Playground, Props } from 'docz';
import { ValidationErrors } from './ValidationErrors.tsx';
import {
  createFakeFieldAPI,
  createFakeLocalesAPI,
  createFakeSpaceAPI,
  ActionsPlayground
} from '@contentful/field-editor-test-utils';

## In Action

<Playground>
  {() => {
    const [field, mitt] = createFakeFieldAPI();
    const locales = createFakeLocalesAPI();
    const space = createFakeSpaceAPI();
    const getEntryURL = (entry)=> `https://example.com/${entry.sys.id}`;

    React.useEffect(()=> {
      mitt.emit('onSchemaErrorsChanged', [{
        message: 'The property "name" is required here',
        name: "required"
      }])
    }, [])

    return (
      <div>
        <ValidationErrors field={field} locales={locales} space={space} getEntryURL={getEntryURL} />
        <ActionsPlayground mitt={mitt} />
      </div>
    );
  }}
</Playground>

## Props

<Props of={ValidationErrors} />
