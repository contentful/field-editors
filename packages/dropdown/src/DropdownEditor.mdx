---
name: Dropdown
route: /dropdown
menu: Components
---

# Dropdown

import { Playground, Props } from 'docz';
import { DropdownEditor } from './DropdownEditor.tsx';
import { createFakeFieldAPI } from '@contentful/field-editor-test-utils';

## In Action

### with proper configuration

<Playground>
  {() => {
    const [field] = createFakeFieldAPI(mock => {
      return {
        ...mock,
        validations: [{ in: ['banana', 'orange', 'strawberry'] }]
      };
    });
    return <DropdownEditor field={field} isInitiallyDisabled={false} />;
  }}
</Playground>

### no configuration

<Playground>
  {() => {
    const [field] = createFakeFieldAPI();
    return <DropdownEditor field={field} isInitiallyDisabled={false} />;
  }}
</Playground>

## Props

<Props of={DropdownEditor} />
