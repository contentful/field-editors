---
name: Dropdown
route: /dropdown
menu: Components
---

# Dropdown

import { Playground, Props } from 'docz';
import { DropdownEditor } from './DropdownEditor.tsx';
import { createFakeFieldAPI } from '@contentful/field-editor-shared';

## In Action

### with proper configuration

<Playground>
  {() => {
    const field = createFakeFieldAPI(mock => {
      return {
        ...mock,
        validations: [{ in: ['banana', 'orange', 'strawberry'] }]
      };
    });
    return <DropdownEditor field={field} initialDisabled={false} />;
  }}
</Playground>

### no configuration

<Playground>
  {() => {
    return <DropdownEditor field={createFakeFieldAPI()} initialDisabled={false} />;
  }}
</Playground>

## Props

<Props of={DropdownEditor} />
