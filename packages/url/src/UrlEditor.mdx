---
name: Url
route: /url
menu: Editors
---

# Url

import { Playground, Props } from 'docz';
import { UrlEditor } from './UrlEditor.tsx';
import { createFakeFieldAPI } from '@contentful/field-editor-test-utils';

## In Action

<Playground>
  {() => {
    const [field] = createFakeFieldAPI();
    return <UrlEditor field={field} isInitiallyDisabled={false} />;
  }}
</Playground>

---

<Playground>
  {() => {
    const [field] = createFakeFieldAPI();
    return (
      <UrlEditor field={field} isInitiallyDisabled={false}>
        {({ value }) => <div>Custom preview for: {value}</div>}
      </UrlEditor>
    );
  }}
</Playground>
