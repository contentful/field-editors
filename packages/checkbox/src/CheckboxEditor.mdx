---
name: Checkbox
route: /checkbox
menu: Components
---

# Checkbox

import { Playground } from 'docz';
import { CheckboxEditor } from './CheckboxEditor.tsx';
import { createFakeFieldAPI } from '@contentful/field-editor-test-utils';

## In Action

<Playground>
  {() => {
    const [field] = createFakeFieldAPI();
    return <CheckboxEditor field={field} isInitiallyDisabled={false} />;
  }}
</Playground>

<Playground>
  {() => {
    const [field] = createFakeFieldAPI(
      mock => ({
        ...mock,
        items: {
          validations: [{ in: ['test1', 'test2', 'test3', 'test4'] }]
        }
      }),
      ['test1', 'test2', 'test3']
    );
    return <CheckboxEditor field={field} isInitiallyDisabled={false} />;
  }}
</Playground>
