---
name: JSON
route: /json
menu: Components
---

# JSON

import { Playground, Props } from 'docz';
import JsonEditor from './JsonEditor.tsx';
import { createFakeFieldAPI } from '@contentful/field-editor-test-utils';

## In Action

<Playground>
  {() => {

    const fieldAPI = createFakeFieldAPI((field, mitt) => {
      window.mitt = mitt;
      window.mitt.on('*', (event, value) => {
        console.log(event, value)
      })
      return field;
    });



    return (
      <div data-test-id="json-editor-integration-test">
        <JsonEditor field={fieldAPI} isInitiallyDisabled={false} />
      </div>
    );

}}

</Playground>
