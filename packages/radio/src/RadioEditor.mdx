---
name: Radio
route: /radio
menu: Components
---

# Radio

import { Playground } from 'docz';
import { RadioEditor } from './RadioEditor.tsx';
import { createFakeFieldAPI } from '@contentful/field-editor-test-utils';

## In Action

<Playground>
  {() => {
    const [field] = createFakeFieldAPI();
    return <RadioEditor field={field} isInitiallyDisabled={false} />;
  }}
</Playground>

<Playground>
  {() => {
    const [field] = createFakeFieldAPI(
      mock => ({
        ...mock,
        type: 'Symbol',
        validations: [{ in: ['test1', 'test2', 'test3', 'test4'] }]
      }),
      ['test1', 'test2', 'test3']
    );
    return <RadioEditor field={field} isInitiallyDisabled={false} />;
  }}
</Playground>
